构建入口为 `make -C doc`，默认目标 `html` 会调用 `doc/src/sgml/Makefile`；可选目标包括 `man`、`postgres.pdf`（需指定 A4/USletter 纸张）、`epub`、`check`（仅 XML 校验）、`distprep`（发布包准备）。依赖工具 `xmllint`、`xsltproc`、`fop`、`dbtoepub`、`pandoc` 可通过环境变量覆盖或自动降级为 `missing`。