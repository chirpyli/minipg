动态共享内存后端类型由配置项 `dynamic_shared_memory_type`（posix/sysv/windows/mmap）决定，并在 `dsm_impl.c` 的 `dynamic_shared_memory_options` 中枚举；PostgreSQL 启动时通过 `dsm_postmaster_startup` 创建全局控制段，其容量按 `PG_DYNSHMEM_FIXED_SLOTS + PG_DYNSHMEM_SLOTS_PER_BACKEND * MaxBackends` 计算。