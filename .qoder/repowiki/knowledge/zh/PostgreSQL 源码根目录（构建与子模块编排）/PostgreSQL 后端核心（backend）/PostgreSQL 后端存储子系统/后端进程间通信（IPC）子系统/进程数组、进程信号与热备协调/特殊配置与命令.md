进程信号子系统需在进程启动阶段调用 `ProcSignalShmemSize`/`ProcSignalShmemInit` 分配共享内存，再由 `ProcSignalInit(pss_idx)` 注册当前进程的槽位；热备模式需先调用 `InitRecoveryTransactionEnvironment` 初始化恢复事务环境，退出前调用 `ShutdownRecoveryTransactionEnvironment` 释放锁与虚拟事务条目。