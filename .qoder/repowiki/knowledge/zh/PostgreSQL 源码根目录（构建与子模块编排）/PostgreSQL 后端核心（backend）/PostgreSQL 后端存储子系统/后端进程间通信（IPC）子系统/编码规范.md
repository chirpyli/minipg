- 每个 IPC 能力以独立 .c 文件组织（如 barrier、latch、pmsignal、shm_mq、sinval 等），并通过 Makefile 的 OBJS 显式注册。
- 跨进程状态统一存放在共享内存段中，由 shmem.c/shmqueue.c 等基础设施分配与映射，其他模块仅通过指针访问。
- 进程间通知采用“共享内存数据结构 + 信号/Latch”的组合模式，避免忙轮询。
- 对外暴露的 IPC 接口以 C 函数形式提供，供 backend 其他子系统调用，而非直接操作共享内存布局。