latch.c 的等待原语可通过编译期宏 WAIT_USE_EPOLL/WAIT_USE_POLL/WAIT_USE_KQUEUE 强制指定，或通过 WAIT_USE_SELF_PIPE/WAIT_USE_SIGNALFD 覆盖默认策略；InitializeLatchSupport 必须在任何 InitLatch/OwnLatch 之前调用以建立 self-pipe/signalfd 和 SIGURG 处理器。