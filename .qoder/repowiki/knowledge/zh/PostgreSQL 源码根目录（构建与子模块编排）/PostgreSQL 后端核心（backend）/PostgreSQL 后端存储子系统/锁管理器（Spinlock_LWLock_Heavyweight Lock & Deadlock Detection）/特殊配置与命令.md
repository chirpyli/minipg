`make check` 会编译并运行 `s_lock_test`（由 `s_lock.c` 在 `-DS_LOCK_TEST=1` 下单独链接 `libpgport` 生成），用于验证自旋锁未卡住；`lwlocknames.h`/`lwlocknames.c` 由 `lwlocknames.txt` 与 `generate-lwlocknames.pl` 自动生成，维护者新增锁时需追加到文件末尾以避免重编号。