- 每个子目录使用独立的 Makefile，通过设置 subdir/top_builddir 并 include Makefile.global 与 common.mk 来复用构建规则。
- nodes 目录按功能维度拆分源文件：copy/equal/out/read/make/nodeFuncs 分别对应节点树的复制、比较、序列化/反序列化、构造与通用操作。
- lib 目录中每个数据结构以单一 .c 文件实现，并在 README 中集中列出所有可用组件，便于外部引用。
- port 目录通过 Makefile 中的条件语句或生成脚本选择特定平台的实现，使上层代码无需感知底层 OS 差异。