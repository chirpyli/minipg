- 每个子目录包含独立的 Makefile 与 objfiles.txt，通过顶层 Makefile 的 SUBDIRS 统一纳入构建。
- 跨子模块共享的数据结构（PlannerInfo、RelOptInfo、Path、Plan、RestrictInfo、EquivalenceClass 等）集中在 util/ 或公共头文件中，由各阶段通过引用头而非直接访问内部状态来协作。
- 优化流程严格遵循 README 定义的单向阶段：prep → path → plan，阶段间通过 PlannerInfo 传递中间结果，禁止逆向回调。