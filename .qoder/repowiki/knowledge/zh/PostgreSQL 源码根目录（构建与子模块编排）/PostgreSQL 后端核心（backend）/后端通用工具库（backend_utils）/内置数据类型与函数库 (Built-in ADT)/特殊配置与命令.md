通过顶层 Makefile.global 引入后使用标准 `make -C src/backend utils/adt` 或 `make -C src/backend world` 进行编译；`objfiles.txt` 列出所有目标文件供外部工具（如 pgindent、文档生成）扫描；`like.o` 额外依赖 `like_match.c`，`varlena.o` 额外依赖 `levenshtein.c`，在 Makefile 中有显式依赖声明。