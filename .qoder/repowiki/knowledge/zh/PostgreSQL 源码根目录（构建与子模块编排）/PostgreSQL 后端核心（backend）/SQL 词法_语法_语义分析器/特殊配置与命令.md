构建时需先运行 Flex/Bison 生成 `scan.c`、`gram.c`、`gram.h`；Makefile 中 `gram.c: BISON_CHECK_CMD = $(PERL) $(srcdir)/check_keywords.pl $< $(top_builddir)/src/include/parser/kwlist.h` 会在生成语法树时校验关键字表，且要求 flex 无回溯（`FLEX_NO_BACKUP=yes`）。