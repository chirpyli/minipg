构建时执行 `make bki-stamp` 会调用 `perl genbki.pl --include-path=... --set-version=<MAJORVERSION>` 扫描 `src/include/catalog/` 下所有目录头与数据文件，生成 `postgres.bki`、`schemapg.h` 与各 `*_d.h`；安装阶段通过 `make install-data` 将 `postgres.bki` 与若干 SQL 脚本复制到 `$(datadir)`。