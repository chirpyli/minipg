- 每个子目录自成独立构建单元，包含自身 Makefile、objfiles.txt 与 README（如适用），顶层 Makefile 仅声明 SUBDIRS 进行编排。
- 具体存储/索引实现通过暴露 TableAmRoutine / IndexAmRoutine / TsmRoutine 等结构体钩子函数与上层交互，而非直接耦合调用。
- 写路径统一经由 transam 子系统生成 WAL 记录，并由 rmgrdesc 提供对应记录类型的文本描述，保证回放与诊断一致性。
- 跨子模块复用的底层数据结构（HeapTuple、ScanKey、TOAST、Relation、TupDesc 等）集中放在 access/common，避免在各 AM 中重复实现。