基于 PostgreSQL 内部 buffer manager（storage/bufmgr）、lock manager（storage/lmgr）、multixact 子系统（access/multixact）、visibility map（access/visibilitymap）以及 WAL 日志（access/xlog*）构建；通过 TableAmRoutine 抽象接入 executor。