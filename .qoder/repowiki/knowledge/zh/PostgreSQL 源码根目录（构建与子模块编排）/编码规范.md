- 顶层 Makefile 统一通过 $(call recurse,<target>,<dirs>,<sub-target>) 宏驱动所有子模块构建，避免手写递归规则。
- 各子模块遵循 PostgreSQL 源码树的目录约定（src/backend、src/bin、src/interfaces、src/common、contrib、doc/src/sgml、src/test），由顶层构建系统自动发现。
- 构建产物与源码分离，config.status 生成的 GNUmakefile 仅作为转发层，实际规则集中在各子目录 Makefile 与 src/Makefile.global。