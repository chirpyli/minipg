隔离测试需额外启用 `max_prepared_transactions >= 3` 并通过 `make check-prepared-txns` 运行；可通过环境变量 `PGISOLATIONTIMEOUT` 调整单步超时；Perl TAP 测试通过 `prove` 执行，支持 `PROVE_TESTS` 指定单个测试文件；各子目录均提供独立的 `make check` / `make installcheck`，顶层 `src/test/Makefile` 汇总运行。