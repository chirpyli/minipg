构建产物均为静态库：`make all` 在 common/port 下分别生成 `libpgcommon.{a,shlib,a,srv}` 与 `libpgport.{a,shlib,a,srv}`，在 fe_utils 下生成 `libpgfeutils.a`；common 需先运行 `make distprep` 以通过 `gen_keywordlist.pl` 生成 `kwlist_d.h`，unicode/ 子目录通过 Perl 脚本生成 Unicode 规范化表。