每个子目录均可独立执行 make check 运行其 t/*.pl TAP 测试；psql 的 sql_help.h/sql_help.c 与 psqlscanslash.c 需在构建时由 create_help.pl 和 flex 自动生成；pg_waldump/pg_rewind 通过符号链接复用 backend 的 xlogreader.c；pg_rewind 的 check 目标在当前裁剪版本中跳过，因为依赖已移除的 pg_basebackup。