- 每个子工具目录自成一体，包含独立的 Makefile、C 源码与 t/*.pl TAP 测试，并通过 PGFILEDESC 声明程序用途。
- 所有工具通过 include src/Makefile.global 并使用 override CPPFLAGS := -DFRONTEND 编译，确保仅作为前端链接 libpgport/libpq/libpgfeutils。
- 大型工具按功能拆分 .c/.h 成对文件（如 psql 的 command/input/prompt/mainloop，pg_rewind 的 file_ops/datapagemap/filemap），并在主入口集中组装。
- 通过符号链接（LN_S）复用 backend 的 xlogreader.c 而非复制源码，保持前后端 WAL 解析逻辑一致。
- 构建产物安装到 $(DESTDIR)$(bindir)，并统一提供 install/uninstall/clean/distclean/maintainer-clean/check/installcheck 目标。